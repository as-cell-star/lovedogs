[build]
builder = "nixpacks"

[deploy]
startCommand = "npx serve dist --single --listen ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
